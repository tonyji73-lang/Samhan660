# 해안 보정 v2 Godot 비교 연결

2026-09-17, main `d953cfa`에서 별도 개발 장면 추가. 기본 게임 장면/지도/규칙/도시 좌표/저장 코드는 변경하지 않았다. 커밋·Push는 이번 요청 범위에 없어 하지 않았다.

## 열기

Godot에서 `res://dev/coast_fix_comparison.tscn`을 열고 **F6(현재 장면 실행)**. F5는 기존 게임을 실행한다. 비교 장면은 캠페인이나 사용자 저장을 불러오지 않는다.

- 상단 버튼: 보정 전후 전환, 기존 영토 마스크·경계, 성·경로 표시, 16개 도시로 이동, 첫 구역/전체 지도.
- 휠: 커서 중심 확대·축소. 빈 지도 왼쪽 드래그: 이동. 성 클릭: 선택 정보/앵커 강조.
- 성 이미지 지면 중심(이미지 정규 좌표 0.5, 0.65)을 도시 UV에 배치했다. 원본 PNG의 투명 여백을 자르거나 파일을 수정하지 않았다. 지역별 최종 성이 아닌 배치 확인용이다.
- 마스크 색은 기존 지역 ID 구분용이며 현재 캠페인 세력 색/지배 상황을 의미하지 않는다. 마스크가 바다인 좌표에서도 성 버튼은 선택되므로 두 검사를 구분한다.

## 원본과 구현

발견한 ZIP: `C:/Users/지용훈/Downloads/Samhan660_Map_Layers_Coast_Fix_v2.zip`.
프로젝트 밖 새 추출 폴더: `C:/Users/지용훈/Downloads/Samhan660_Map_Layers_Coast_Fix_v2_review_20260917/Samhan660_Map_Layers_Coast_Fix_v2/`.
기존 폴더 덮어쓰기 없이 압축 해제했고 제공된 SHA256SUMS의 파일 해시를 대조했다. README_KO, VSCODE_NEXT_STEP, override spec, 24구역 manifest를 읽고 작업했다.

프로젝트 사본은 `assets/map_review/coast_fix_v2/`. 보정 전/후·성 PNG와 원본 명세/좌표 스냅샷을 별도 경로에 보존했다. 묶음 내부의 경로를 기술한 원본 명세는 수정하지 않았으며 비교 코드는 이 새 경로의 PNG를 명시적으로 읽는다. PNG 바이트는 원본 그대로이고 Godot 임포트에서 mipmap/선형 필터만 적용했다.

`dev/coast_review_map.gd`는 기존 `map_area.gd`를 상속해 실제 지도 배경·마스크·경로·좌표 병합·확대/이동 변환을 재사용한다. 논리 6144×4096의 (3072,2048)에 1024×1024로 보정 구역을 배치한다. 실제 PNG는 1254×1254이며 확대 단계마다 배경을 바꾸지 않는다. `effective_coordinates.json`은 비교 검사에만 사용하고 게임 좌표를 덮어쓰지 않는다.

새 파일: 위 자산 경로, `dev/coast_fix_comparison.tscn`, `dev/coast_fix_comparison.gd`, `dev/coast_review_map.gd`, `tests/coast_fix_comparison_test.gd`, 이 기록 및 해당 Godot UID/임포트 메타데이터. 기존 추적 파일의 diff는 없다.

## 실제 검증

Godot 4.7.2, D3D12, 1280×720 / 1920×1080 실제 창에서 GUI 자동 입력. `Godot --path . --script res://tests/coast_fix_comparison_test.gd`. **159개 검사, 실패 0**, 스크립트 오류 없음.

- 54개 런타임 병합 좌표와 제공된 진단 스냅샷 일치. 실행 후 좌표 불변.
- 두 해상도에서 보정 전후 버튼, 실제 휠 누름/해제 입력, 지도 드래그, 첫 구역 16개 성 각각 클릭.
- 선택한 성의 화면 좌표 역변환 오차 0.000001 UV 미만. 기존 경로 양 끝과 도시 앵커 일치.
- 줌 1~5 모두 보정 구역 UV 시작 (0.5,0.5), 크기 (1/6,1/4), 동일 보정 텍스처 유지. 전후 전환으로 도시 위치가 바뀌지 않음.
- 성 숨김 상태의 원본 지형 전후와 영토 마스크 중첩을 실제 렌더 캡처로 검토.
- 초기 자동 입력 실패는 합성 휠의 해제 이벤트 누락으로 GUI 마우스 캡처가 남던 시험 문제였다. 실제 휠처럼 누름/해제 쌍으로 수정했고, 비교 장면은 해제 이벤트로 확대가 두 번 적용되지 않도록 처리한다. 기본 게임 입력 코드는 수정하지 않았다.

사람의 수동 플레이 검증과 구분한다. 기본 사용자 저장의 SHA-256 전후 동일:
`3bca5c5286be3abb44ed60fbb9e1efe39815c29f6ff255d2ad825273aa5275d3`.

## 지형·영토 결과와 남은 문제

| 앵커 | 보정 후 지면/기존 마스크 |
|---|---|
| 금관가야 (0.57,0.69) | 보정 전 바다 → 보정 후 육지. 기존 마스크는 **ID 0(바다)**여서 영토 표현 불일치 |
| 소가야 (0.54,0.69) | 보정 전 해상/섬 인근 → 보정 후 연결 육지. 앵커 마스크 ID 30(소가야) 일치. 주변 새 해안 전체 일치는 아님 |
| 울릉 (0.62,0.55) | 보정 후 섬 내부. 기존 마스크는 **ID 0(바다)**여서 영토 표현 불일치 |
| 달구벌·금성·사벌 | 선택/좌표와 마스크 ID 27/25/26 일치. 달구벌 주변 하천·평지 아트가 달라졌고 금성은 해안 가까움. 배치 적합성 최종 승인 아님 |
| 북한산·삼년산성·국원·실직·웅진·사비·고사·대가야·죽령·추풍령 | 선택/좌표와 해당 지역 마스크 ID 일치. 북한산은 구역 상단 경계, 삼년산성/국원/죽령 등은 산능선·숲에 인접. 나머지 앵커의 평탄한 성터까지 확보됐다고 판단하지 않음 |

총 16개 중 **14개 앵커의 기존 마스크 ID가 자기 지역과 일치**, 금관가야·울릉 2개는 바다다. 이는 앵커 한 점 검사이며 지역 면 전체나 영토 클릭 영역의 정합 통과가 아니다. 시각적 전후 비교에서도 새 남쪽 육지와 기존 마스크 해안이 넓게 어긋난다.

주변 전체 지도와 첫 구역 사이에 직사각형 테두리를 따라 색·조명·해안선 단절이 뚜렷하다. 전체 지도 바탕은 기존 그림이고 상세 구역은 채도가 높은 별도 그림이어서 자동 접합 완료로 취급할 수 없다. 인접 23구역은 제공되지 않았다. 기존 경로 좌표는 유지되므로 아트의 길/하천/해안과 일치하도록 재제작할 부분도 남는다. 기본 지도 채택/영토 마스크 수정은 하지 않았다. 최대 확대에서 1254px 원본 선명도와 성터/건축 적합성은 추가 아트 검토가 필요하다.

## 실제 캡처

생성물은 `.godot/coast-review/`에 있으며 Git 미포함이다. 캐시 정리 전 필요한 PNG를 복사하면 된다.

- `1280-after.png`, `1280-before.png`: 두 상태 동일 범위.
- `1920-land-after.png`, `1920-land-before.png`: 성/경로를 숨긴 16개 앵커 지면 비교.
- `1920-mask.png`: 기존 마스크·경계 중첩.
- `1920-geumgwan.png`, `1920-sogaya.png`, `1920-ulleung.png`, `1920-geumseong.png`, `1920-dalgubeol.png`, `1920-sabeol.png`: 성 선택 상세.
- `1280-overview.png`, `1920-overview.png`: 저배율에도 유지되는 같은 구역.
- `result.json`: 검사 결과와 16개 앵커 UV/기존 마스크 ID.

과거 원본 저장 재검증 2건 등 기존 미검증 사항은 이번 지도 장면 검사로 대체하지 않는다.
