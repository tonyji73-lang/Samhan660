# 중부·동해 r3 보충 원화 연결

2026-09-22. 현재 main과 기존 미커밋 변경 보존. Downloads/Samhan660_Central_East_Detail_v1.zip의 VSCODE_REQUEST.txt, START_HERE_KO.md, integration/R3_INTEGRATE_KO.md를 읽고 적용했다. 기존 r2 PNG와 망은 ZIP과 바이트가 같음을 확인하고 그대로 유지했다. 패키지 보조 스크립트로 현재 검증된 코드를 덮어쓰지 않았다.

## 연결

- approved_korea_map.gd: 별도 _r3_fill 보조 객체, 새 PNG와 새 fill JSON만 연결. 기존 런타임 밉맵 정책 유지.
- 그리기 순서: 승인 지도 → r3 fill(선택한 경우) → 기존 r2 → 영토·지원 경로·성·글자. r3 끄기는 r2로 복귀하며 원본/미보정 모드로 바꾸면 r3 상태도 해제한다.
- settlement_overlay.gd: 왼쪽 r3 보충 / r2 복귀 버튼. 기존 비교 카메라 유지 기능 보존.
- 새 자산: ui/korea_layout_v1/central_east/assets/central_east_detail_r3.png, data/central_east_r3_fill_mesh.json. R3_INTEGRATE_KO.md와 R3_REVIEW_KO.md도 보관.
- 자동 LOD는 기존 manifest의 false 유지. 성 좌표·크기·규칙·저장 형식 변경 없음. 세력 선택 UI는 변경하지 않았다.

F5 → 642년 신라 → 거점 지도 → 중부·동해 → 보정 보기(r2) → 왼쪽 r3 보충. r2 복귀로 같은 위치에서 비교한다.

PNG SHA256:58b71825b93401688b247204725dd2d35537209e3cff12411beb3b75ef516cb3.
fill JSON SHA256:c343f08992ddd01be55b5e9b723142103083f5825c2e3a2e910997fc57d956f6.

## 실행 범위

Godot4.7.2 D3D12, 642 신라, tests/central_east_r3_test.gd GUI 자동 입력 및 실제 엔진 캡처. 사람의 수동 플레이가 아니다. 두 해상도1280×720/1920×1080, 줌1/5/10 원본·미보정·r2·r3 비교를 수행한다. 주 비교는 같은 위치의 줌5다.
실제 망의 모든 정점에서 UV=위치/(264,176)을 검사하여 r2 변형 UV가 재적용되지 않음을 확인한다. 실제 로드1536×1024, r3 끄기/r2 복귀, 6개 거점 선택, 지원 경로, 하단 UI, 재열기 카메라 유지, 자동 전환 차단과 캠페인 상태 보존을 검사한다.
초기 검사 코드에서 2D 망의 Vector2를 Vector3로 가정한 오류를 수정하고 재실행했다. 최종 결과:.godot/r3-final.log 및 .godot/central-east-r3/result.json.
캡처:-corrected.png는 r2, -r3.png는 r3 보충. {1280,1920}-z5-*가 같은 위치·배율 비교다. 패키지 계산 이미지를 Godot 증거로 사용하지 않았다.
사용자 기본 저장 SHA256은 기존3bca5c5286be3abb44ed60fbb9e1efe39815c29f6ff255d2ad825273aa5275d3과 동일하다. 이번에 저장 복원·전체 캠페인 회귀·사람의 수동 플레이는 수행하지 않았다.

## 최종 결과와 육안 검토

최종 실제 실행 **70 checks, 0 failures**. 최종 로그에 오류·경고 없음. 이 횟수는 이번 r3 실행 결과이며 r2의56건을 재사용한 것이 아니다. Godot 화면에서 원본→r3→r2→성/경로 순서와 정점 투명도의 결과를 확인했다.

- 국원 남쪽 물길: 두 해상도5배율에서 r2의 흐린 물길보다 r3의 강둑·수면 경계가 선명하다. 국원 남쪽을 가로질러 동쪽 합류부에 이르는 구간은 얇고 또렷한 선으로 바뀐다. 이는 선명도 개선이지 위치 일치의 증명이 아니다.
- 승인1254 기준 대략 x592~686,y690~720의 남쪽 강: 폭과 작은 분기의 모양이 r2/원본과 다르다. 특히 아래쪽 출구에서 고해상도 물길이 흐린 원본으로 섞이는 변화가 남는다. 화면 전체에서 물길 이중 윤곽이 없다고 보증하지 않는다. 비교한5배율에서 가장 뚜렷한 잔여 문제는 이 출구의 흐림·폭 변화다.
- 북한산성 합류점과 당항성 만·반도: 해안과 강둑 경계는 또렷해졌지만 작은 지류·섬·물가 전체의 위치 정합을 검증한 것은 아니다.
- 하슬라~실직 해안: 성 위치는 그대로이며 해안 묘사가 선명해진다. 상세 사각형 가장자리의 원본 혼합과 산기슭 해상도 차이는 남는다. 일부 성 주변 지면/물가의 전체 바닥 안전성은 미검증이다.

위 결과는 실제 캡처의 육안 비교다. 정밀 지리 측량·35곳 성 바닥 전체·영토 마스크9곳·세계 지도 접합·전체 확대 해결은 완료 처리하지 않는다. 과거 원본 저장 재검증2건도 미검증 유지. 자동 LOD는 계속 false이다.
