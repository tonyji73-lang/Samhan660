# 세력 선언 복원 v1 — 2026-09-22

## 적용 파일

- `ui/faction_declaration_v1/declarations.json`, `declaration_catalog.gd`, `hanji_declaration.gd`: 다운로드 ZIP의 선언 전용 자료를 추가했다.
- `ui/faction_selection_v1/faction_selection.gd`: 실제 scenario_id/faction_id와 군주 이름을 대조하여 선언을 조회한다. 없는 항목은 다른 세력/연도의 선언으로 대체하지 않는다. 기존 한지 배경·사각형은 유지하고 고정 글자·인장만 투명 선언 컴포넌트로 교체했다.
- 전체 stage 재생성 전에 capture_state(), 새 선언 생성 후 restore_state()를 호출한다. 동일 선택의 난이도·모드 변경은 진행도를 유지하고, 다른 시나리오/세력은 새 글로 시작한다. 제거한 stage는 트리에서 분리 후 해제하여 이전 글이 갱신되지 않는다. 창 크기 변경은 기존 stage 스케일 조정을 사용한다.
- `tests/faction_declarations_v1_test.gd`: 실제 GUI 자동 입력 검사와 캡처.

## 검증

- ZIP reference/ruler_declarations_v1_ko.txt와 JSON의 16개 키, 다섯 줄, 공백, 순서를 직접 대조: 모두 일치. 인물은 게임용 창작 대사이며 실제 어록으로 안내하지 않는다.
- Godot 4.7.2 실제 GUI: 1206 assertions, 0 failures. 다수 assertion은 개별 글자의 영역·표시 순서 검사이며 독립적인 1206개 플레이 시나리오를 의미하지 않는다.
- 선택 가능 12개 조합: 632·642·660년 신라/백제/고구려, 663년 신라/고구려, 670년 신라. 1280×720과 1920×1080에서 각 조합을 자연 재생 완료 후 캡처했다.
- 오른쪽→왼쪽 열, 위→아래 글자, 한지 영역 내 배치, 전체 문구 유지, 인장까지 재생 완료를 검사했다. 글자/인장 잘림 없이 기존 한지 안에 표시된다.
- 재생 중 난이도·모드 변경 직전/직후 경과 시간 동일, 완료 후 해상도 변경 시 완료 상태 유지, 빠른 연도·고구려 연속 선택 시 새 선언으로 초기화, 뒤로/재열기, 재생 중 642년 신라 캠페인 시작을 확인했다.
- 잠긴 4개 선언(663 백제부흥/당, 670 당/고구려부흥)은 데이터·실제 군주 대응만 검사했고 게임 화면에서 강제로 선택하거나 해금하지 않았다.
- 로그 `.godot/declarations-gui.log`; 캡처 `.godot/declarations-v1/{1280,1920}-{연도}-{세력}.png` (24장). 최종 실행 스크립트/렌더 오류 없음. 최초 검사 스크립트의 상속 상수명 충돌은 수정 후 재실행했다.
- 실제 실행은 GUI 자동 입력이며 사람의 수동 플레이는 미검증이다. 기존 회귀 숫자를 재사용하지 않았다.

## 보존

초상/보장왕 663 남색 PNG, 간접 지도, 시작 거점의 세력별 색·위치, 잠금 툴팁, 게임 규칙과 사용자 저장은 변경하지 않았다.
보장왕 PNG SHA-256: aaef1a42417a68abdb774a3ff87eb57078f144eaca8291a12e130ab508a2c7c2.
사용자 저장 SHA-256: 3bca5c5286be3abb44ed60fbb9e1efe39815c29f6ff255d2ad825273aa5275d3.
